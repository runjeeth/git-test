{
  "name": "git-test",
  "version": "1.0.0",
  "description": "this is test run",
  "main": "index.html",
  "scripts": {
    "start": "npm run lite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lite":"lite-server"
  },
  "author": "runjeeth nikam",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.5.4"
  }
}
